"""Auction router — CRUD + history."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/auctions", tags=["auctions"])


# Stub — bound for senior-engineer implementation
