"""WebSocket router — WS /auctions/{id}/live."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["websocket"])


# Stub — bound for senior-engineer implementation
