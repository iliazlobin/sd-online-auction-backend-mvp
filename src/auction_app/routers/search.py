"""Search router — GET /auctions (search/filter)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/auctions", tags=["search"])


# Stub — bound for senior-engineer implementation
